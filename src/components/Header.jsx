function Header() {
    return(
        <header>
            <h1>Archive Style Gram</h1>

            <nav>
                <a href="#">Search</a>
                <a href="#">Home</a>
                <a href="#">Publish</a>
            </nav>
        </header>
    );
}

export default Header;